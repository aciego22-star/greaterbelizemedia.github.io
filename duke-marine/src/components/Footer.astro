---
import Logo from './Logo.astro';
import Icon from './Icon.astro';
import { site } from '../data/site';
---

<footer class="footer on-dark">
  <svg class="divider-wave" viewBox="0 0 1440 60" preserveAspectRatio="none" aria-hidden="true">
    <path
      d="M0 30c120 20 240 30 360 25s240-25 360-25 240 20 360 20 240-15 360-20v40H0Z"
      fill="var(--c-navy)"></path>
  </svg>

  <div class="footer__top container container-wide">
    <div class="footer__brand">
      <Logo />
      <p class="footer__mission">
        {site.description}
      </p>
      <div class="footer__social">
        <a href={site.social.facebook} aria-label="Facebook" class="social"><Icon name="facebook" size={20} /></a>
        <a href={site.social.instagram} aria-label="Instagram" class="social"><Icon name="instagram" size={20} /></a>
        {site.social.youtube && (
          <a href={site.social.youtube} aria-label="YouTube" class="social"><Icon name="youtube" size={20} /></a>
        )}
        <a href={`https://wa.me/${site.whatsapp}`} aria-label="WhatsApp" class="social"><Icon name="whatsapp" size={20} /></a>
      </div>
    </div>

    <nav class="footer__nav" aria-label="Footer">
      <div>
        <h3 class="footer__heading">Explore</h3>
        <ul role="list">
          <li><a href="/">Home</a></li>
          <li><a href="/marine-supplies">Marine Supplies</a></li>
          <li><a href="/fishing-supplies">Fishing Supplies</a></li>
          <li><a href="/diving-supplies">Diving Supplies</a></li>
          <li><a href="/brands">Brands We Carry</a></li>
          <li><a href="/commercial">Commercial &amp; Wholesale</a></li>
          <li><a href="/services">Services</a></li>
        </ul>
      </div>
      <div>
        <h3 class="footer__heading">Company</h3>
        <ul role="list">
          <li><a href="/about">About Us</a></li>
          <li><a href="/gallery">Gallery &amp; Video</a></li>
          <li><a href="/blog">Blog</a></li>
          <li><a href="/careers">Careers</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </div>
      <div class="footer__contact">
        <h3 class="footer__heading">Visit &amp; Contact</h3>
        <ul role="list">
          <li>
            <Icon name="pin" size={18} />
            <span>{site.address.line1}, {site.address.city}, {site.address.country}</span>
          </li>
          <li>
            <Icon name="phone" size={18} />
            <a href={`tel:${site.phoneHref}`}>{site.phoneDisplay}</a>
          </li>
          <li>
            <Icon name="mail" size={18} />
            <a href={`mailto:${site.email}`}>{site.email}</a>
          </li>
          <li>
            <Icon name="clock" size={18} />
            <span>{site.hours[0].day}: {site.hours[0].time}</span>
          </li>
        </ul>
      </div>
    </nav>
  </div>

  <div class="footer__bottom container container-wide">
    <p>© <span data-year>2026</span> {site.legalName}. All rights reserved.</p>
    <p class="footer__credit">
      Crafted by <a href="https://greaterbelizemedia.github.io" rel="noopener">Greater Belize Media</a>
    </p>
  </div>
</footer>

<style>
  .footer {
    background: var(--c-navy);
    color: var(--text-invert);
    position: relative;
    margin-top: auto;
  }
  .divider-wave {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 60px;
    transform: translateY(-99%);
  }
  .footer__top {
    display: grid;
    grid-template-columns: 1.3fr 2fr;
    gap: clamp(2rem, 5vw, 5rem);
    padding-block: clamp(3.5rem, 6vw, 5.5rem) 3rem;
  }
  .footer__mission {
    margin-top: 1.2rem;
    max-width: 34ch;
    color: #ffffff;
    font-size: 0.95rem;
  }
  .footer__social {
    display: flex;
    gap: 0.6rem;
    margin-top: 1.5rem;
  }
  .social {
    display: grid;
    place-items: center;
    width: 42px;
    height: 42px;
    border-radius: 50%;
    border: 1px solid var(--border-strong);
    color: var(--text-invert);
    transition: transform var(--dur), background var(--dur), color var(--dur);
  }
  .social:hover {
    transform: translateY(-3px);
    background: var(--c-current);
    color: var(--c-navy);
    border-color: transparent;
  }
  .footer__nav {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .footer__heading {
    font-size: 0.8rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #ffffff;
    margin-bottom: 1rem;
  }
  .footer__nav ul {
    display: grid;
    gap: 0.7rem;
  }
  .footer__nav a,
  .footer__contact span {
    color: #ffffff;
    font-size: 0.92rem;
    transition: color var(--dur);
  }
  .footer__nav a:hover {
    color: #fff;
  }
  .footer__contact li {
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
    color: #ffffff;
  }
  .footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
    padding-block: 1.5rem;
    border-top: 1px solid var(--border);
    font-size: 0.85rem;
    color: #ffffff;
  }
  .footer__credit a {
    color: #ffffff;
  }
  .footer__credit a:hover {
    color: #fff;
  }

  @media (max-width: 860px) {
    .footer__top {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 560px) {
    .footer__nav {
      grid-template-columns: 1fr 1fr;
    }
    .footer__contact {
      grid-column: 1 / -1;
    }
  }
</style>
