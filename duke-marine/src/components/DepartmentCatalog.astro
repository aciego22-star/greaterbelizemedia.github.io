---
import PageHero from './PageHero.astro';
import CategoryCard from './CategoryCard.astro';
import ProductCard from './ProductCard.astro';
import Icon from './Icon.astro';
import CtaBanner from './CtaBanner.astro';
import { departments, type DepartmentId } from '../data/catalog';
import { productsByDepartment } from '../data/products';

interface Props {
  dept: DepartmentId;
}
const { dept } = Astro.props;
const d = departments[dept];
const items = productsByDepartment(dept);
const others = (Object.keys(departments) as DepartmentId[])
  .filter((id) => id !== dept)
  .map((id) => departments[id]);

// Authentic client lifestyle photo behind each department header.
// `mobile` is an optional portrait crop for narrow screens: a wide photograph
// cover-cropped into a tall header loses its subject, so departments with a
// clear subject supply a crop composed for portrait.
const heroImageFor: Record<
  DepartmentId,
  { src: string; mobile?: string; pos?: string; alt?: string }
> = {
  marine: { src: '/media/life/boat-wake.jpg', pos: 'center 40%' },
  fishing: {
    src: '/media/life/fishing-catch.jpg',
    mobile: '/media/life/fishing-catch-mobile.jpg',
    pos: 'center 30%',
    alt: 'An angler holding a kingfish aboard a boat offshore of Belize',
  },
  diving: {
    src: '/media/life/diving-turtle.jpg',
    mobile: '/media/life/diving-turtle-mobile.jpg',
    pos: 'center 50%',
    alt: 'A diver swimming alongside a green sea turtle over the seagrass flats in Belize',
  },
};

// group products by category id for anchored sections
const byCategory = d.categories.map((c) => ({
  category: c,
  products: items.filter((p) => p.category === c.id),
}));
---

<PageHero
  eyebrow="Shop the range"
  title={d.title}
  lead={d.tagline}
  crumbs={[{ label: d.title }]}
  image={heroImageFor[dept].src}
  mobileImage={heroImageFor[dept].mobile}
  imageAlt={heroImageFor[dept].alt ?? `${d.title} at Duke Marine, Belize City`}
  imagePosition={heroImageFor[dept].pos}
>
  <div class="dhero__actions" data-reveal data-reveal-delay="180">
    <a href="#categories" class="btn btn--gold">Browse categories <Icon name="arrow" size={18} /></a>
    <a href="/contact#quote" class="btn btn--ghost">Request a Quote or Contact</a>
  </div>
</PageHero>

<!-- Categories -->
<section id="categories" class="section">
  <div class="container">
    <div class="grid auto-fit" style="--min: 260px">
      {d.categories.map((c, i) => <CategoryCard category={c} href={`#${c.id}`} delay={i * 60} />)}
    </div>
  </div>
</section>

<!-- Product listing grouped by category (anchored) -->
<section class="section section-tight" style="background: var(--bg-inset)">
  <div class="container">
    {
      byCategory.map(({ category, products }) => (
        <div class="catgroup" id={category.id}>
          <div class="catgroup__head" data-reveal>
            <div>
              <span class="eyebrow"><Icon name={category.icon} size={16} /> {category.name}</span>
              <p class="catgroup__blurb">{category.blurb}</p>
            </div>
            <a href="/contact#quote" class="btn btn--ghost btn--sm">Ask about {category.name.split(' ')[0].toLowerCase()} <Icon name="arrow" size={15} /></a>
          </div>
          {products.length > 0 ? (
            <div class="grid auto-fit" style="--min: 250px">
              {products.map((p, i) => <ProductCard product={p} delay={(i % 4) * 60} />)}
            </div>
          ) : (
            <div class="catgroup__empty" data-reveal>
              <p><b>In stock.</b> We carry a range of {category.name.toLowerCase()}, including {category.examples.slice(0, 3).join(', ')} and more.</p>
              <a href="/contact#quote" class="link-arrow">Ask us what’s available <Icon name="arrow" size={16} /></a>
            </div>
          )}
        </div>
      ))
    }

    <p class="catnote" data-reveal>
      <Icon name="tag" size={18} /> Don’t see it? We carry a wide range in store,
      <a href="/contact">just ask</a>.
    </p>
  </div>
</section>

<!-- Cross-sell to the other departments -->
<section class="section">
  <div class="container">
    <span class="eyebrow crossell__eyebrow" data-reveal>Also at Duke Marine</span>
    <div class="crossell-grid">
      {
        others.map((o, i) => (
          <a href={o.href} class="crossell" data-reveal data-reveal-delay={i * 90}>
            <div class="crossell__media">
              <img
                src={heroImageFor[o.id].src}
                alt={heroImageFor[o.id].alt ?? `${o.title} at Duke Marine, Belize City`}
                class="crossell__img"
                loading="lazy"
                style={heroImageFor[o.id].pos ? `object-position:${heroImageFor[o.id].pos}` : undefined}
              />
            </div>
            <div class="crossell__body">
              <h3>{o.title}</h3>
              <p>{o.tagline}</p>
              <span class="link-arrow">Explore <Icon name="arrow" size={16} /></span>
            </div>
          </a>
        ))
      }
    </div>
  </div>
</section>

<CtaBanner />

<style>
  .dhero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    margin-top: 2rem;
  }
  .catgroup {
    scroll-margin-top: 100px;
    padding-block: clamp(1.8rem, 4vw, 3rem);
    border-bottom: 1px solid var(--border);
  }
  .catgroup:last-of-type {
    border-bottom: 0;
  }
  .catgroup__head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1.5rem;
    flex-wrap: wrap;
    margin-bottom: 1.6rem;
  }
  .catgroup__blurb {
    margin-top: 0.6rem;
    max-width: 60ch;
    color: var(--text-soft);
  }
  .catgroup__empty {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    flex-wrap: wrap;
    padding: 1.6rem 1.8rem;
    background: var(--bg-elevated);
    border: 1px dashed var(--border-strong);
    border-radius: var(--radius-lg);
  }
  .catgroup__empty p {
    color: var(--text-soft);
    max-width: 62ch;
  }
  .catnote {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    justify-content: center;
    margin-top: 2.5rem;
    color: var(--text-soft);
  }
  .catnote a {
    color: var(--accent-strong);
    font-weight: 600;
  }
  .catnote :global(svg) {
    color: var(--accent-strong);
  }

  .crossell__eyebrow {
    display: block;
    margin-bottom: 1.4rem;
  }
  .crossell-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.6rem;
  }
  .crossell {
    display: flex;
    flex-direction: column;
    background: var(--grad-deep);
    color: var(--text-invert);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    transition: transform var(--dur) var(--ease-out), box-shadow var(--dur);
  }
  @media (hover: hover) and (pointer: fine) {
  .crossell:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow-lg);
  }
}
  .crossell__media {
    overflow: hidden;
  }
  .crossell__img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
    transition: transform var(--dur-slow) var(--ease-out);
  }
  @media (hover: hover) and (pointer: fine) {
  .crossell:hover .crossell__img {
    transform: scale(1.05);
  }
}
  .crossell__body {
    padding: 1.4rem 1.6rem 1.6rem;
  }
  .crossell__body h3 {
    font-size: 1.4rem;
  }
  .crossell__body p {
    margin: 0.4rem 0 1rem;
    color: #cfe4e6;
  }
  .crossell__body .link-arrow {
    color: var(--c-current-bright);
  }
  @media (max-width: 680px) {
    .crossell-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
